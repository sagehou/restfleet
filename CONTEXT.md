# RestFleet 数据安全用语

这里只约定产品术语；安全规则与实现决策分别见 `docs/spec/` 和 `docs/adr/`。

## Language

**备份会话**：一个 Host 在自身 Repository 上执行一次已授权备份的有限生命周期。不同备份及其重试不共享临时锁归属。
_Avoid_：Agent 在线连接、永久网关身份

**临时锁清理**：正常备份刷新锁或结束时，移除属于本 Host、本次备份会话的临时锁。
_Avoid_：维护解锁、删除任意 locks 对象

**维护解锁（unlock）**：由中心在确认没有活动备份后，对遗留锁进行预览、审计和清理的维护操作。
_Avoid_：备份结束清理、Agent 自助修复

**备份占用**：某个 Host 的备份数据面已获准独占自身仓库及相应存储凭据的状态。授权到期不等于旧备份已经退出；只有确认清理完成，才允许维护或新的占用进入。
_Avoid_：备份成功、网关就绪、自动过期的维护通行证

**离线备份授权**：中心预先授予特定 Host 在自身仓库上进行备份的有期限许可，控制面失联时仍可在剩余期限内使用。它不是新的配置接受确认，也不是维护权限。
_Avoid_：永久授权、离线续期、占用释放证明

**待回写记录**：控制面暂不可用时已由 Gateway 可靠保存、等待中心验证接收的审计或凭据刷新记录。它不表示中心权威状态已经更新。
_Avoid_：已入库审计、已生效凭据版本
